# Server
PORT=3001
NODE_ENV=development

# PostgreSQL + PostGIS
DB_HOST=localhost
DB_PORT=5432
DB_NAME=fleet_db
DB_USER=postgres
DB_PASSWORD=postgres

# JWT
JWT_SECRET=your_super_secret_jwt_key_change_in_production
JWT_EXPIRES_IN=7d

# Mapbox (for server-side routing)
MAPBOX_TOKEN=test

# WebSocket
WS_PORT=3002

# Simulation
SIMULATE_VEHICLES=true
SIMULATION_INTERVAL_MS=3000
